<%- include('header', {title: `Sightings by Shapes`}); -%>

  <h1>
    <%= results[0].shape %>
  </h1>
  <table class="table table-striped table-sm table-responsive-md">
    <thead style="text-align: center;">
      <tr>
        <th scope="col">Report</th>
        <th scope="col">Summary</th>
      </tr>
    </thead>
    <tbody>
      <!-- Create an row for each sighting -->
      <% for (let i=0; i < results.length; i +=1) {%>
        <div class="outer">
          <tr class="shape-text">
            <th scope="row" style="text-align: center;"" class=" theader">
              <%= i +1 %>
            </th>
            <% for (let j=0; j < sightings.length; j +=1) { %>
              <% if (sightings[j].summary===results[i].summary) { %>
                <td class="shape-report">
                  <a href="<%= `/sighting/${i + 1}` %>">
                    <%= results[i].text %>
                  </a>
                </td>
          </tr>
          <% }; %>
            <% }; %>
        </div>
        <% }; %>
    </tbody>
  </table>


  <%- include('footer'); -%>